kind: Ingress
apiVersion: networking.k8s.io/v1
metadata:
  name: orchestrator-mongodb-websockify
  annotations:
    cert-manager.io/cluster-issuer: letsencrypt-prod
    kubernetes.io/ingress.class: nginx
    nginx.ingress.kubernetes.io/proxy-read-timeout: "3600"
    nginx.ingress.kubernetes.io/proxy-send-timeout: "3600"
    nginx.ingress.kubernetes.io/proxy-request-buffering: "off"
    nginx.ingress.kubernetes.io/force-ssl-redirect: "true"
spec:
  tls:
    - hosts:
        - mongodb-admin.orchestrator.fedcomp.eucaim.cancerimage.eu
      secretName: mongodb-admin.orchestrator.fedcomp.eucaim.cancerimage.eu
  rules:
    - host: mongodb-admin.orchestrator.fedcomp.eucaim.cancerimage.eu
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: orchestrator-mongodb-websockify
                port:
                  number: 80