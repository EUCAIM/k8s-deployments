apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: orchestrator-mongodb-data
spec:
  storageClassName: managed-nfs-storage
  accessModes:
    - ReadWriteMany
  resources:
    requests:
      storage: 20Gi