apiVersion: v1
kind: ServiceAccount
metadata:
  name: sa-user-orchestrator
  namespace: fedcomp-users
annotations:
  description: The user that has access to the namespace and can edit the resources
#automountServiceAccountToken: false

---

kind: Role
apiVersion: rbac.authorization.k8s.io/v1
metadata:
  name: sa-user-orchestrator
  namespace: fedcomp
annotations:
  description: Rigths for the user's SA
rules:
  - apiGroups:
        - ""
        - apps
    resources: ["secrets", "pods", "events", "pods/log", "pods/status", "pods/exec", "configmaps", "deployments", "deployments/scale", "deployments/status", "services", "services/status", "replicasets"]
    verbs: ["create", "get", "list", "watch",  "delete", "update", "patch"]

---

kind: RoleBinding
apiVersion: rbac.authorization.k8s.io/v1
metadata:
  name: sa-user-orchestrator
annotations:
  description: Binds the role for the user SA to the actual role
subjects:
  - kind: ServiceAccount
    name: sa-user-orchestrator
    namespace: fedcomp-users
roleRef:
  kind: Role
  name: sa-user-orchestrator
  apiGroup: rbac.authorization.k8s.io

---

apiVersion: v1
kind: Secret
metadata:
  name: sa-user-orchestrator-token
  namespace: fedcomp-users
  annotations:
    kubernetes.io/service-account.name: sa-user-orchestrator
annotations:
  description: Predefined token for the user SA
type: kubernetes.io/service-account-token


