apiVersion: v1
kind: Service
metadata:
  name: dashboard-mongodb-websockify
spec:
  selector:
    app: dashboard-mongodb-websockify
  ports:
  - port: 80
    targetPort: 80
    protocol: TCP
  type: ClusterIP
