apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: dashboard-vault-data-config
spec:
  storageClassName: managed-nfs-storage
  accessModes:
    - ReadWriteMany
  resources:
    requests:
      storage: 20Gi